export const capitalize = (word) => {
    return word.charAt(0).toUpperCase() + word.slice(1);
}

export const allItems = {
    challenges: [
        'fizz-buzz', 
        'anagram', 
        'fibonacci', 
        'prime-number',
        'polygon-area'
    ],
    apps: ['app1', 'app2', 'app3']
}